# About
this design pattern emphasis making your project in terms of props getter

# Note
this pattern does not have a standard way or implementations because any component/function returns a props as a function is a getter function which can be consumed, also this pattern is not so good because you have to know about the props and the component in order to use it
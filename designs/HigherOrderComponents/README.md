# About
this design pattern emphasis making your project in terms of:
1. normal component: it is just a simple or normal component
2. higher-order component: it is a component that wraps another component in some way usually it takes it as parameter and returns a new component